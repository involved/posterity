<div class="attachment">
	Image...
	<%= image_tag @attachment.image.square.url if @attachment.image && @attachment.image.square && @attachment.image.square.url %><br />
	<%= @attachment.created_at %>
</div>