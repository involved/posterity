<%= simple_form_for [:manage, Attachment.new], :html => {:multipart => true, :class => "remote_image_upload", :'data-type' => 'json'}, :remote => true do |f| %>
	<%= f.input :image, :as => :file, :required => false, :input_html => {:"data-submit-on-change" => true} %>
	<%#= f.button :submit, "Upload" %>
<% end %>